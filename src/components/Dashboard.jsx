function Dashboard({ tasks, notes, getToday, completionPercent, setView, setSelectedDate, setPriorityFilter, setSortBy, exportData, importData, resetData }) {
    return (
        <section>
            <h2>Dashboard</h2>
        </section>
    )
}

export default Dashboard;